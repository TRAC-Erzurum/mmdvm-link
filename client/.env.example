# MMDVM-Link client — kopyala: cp .env.example .env ve değerleri doldur

# Pre-signed (sunucu tarafından üretilen installer ile doldurulur)
SERVER_ADDR=
NODE_ID=
AUTH_TOKEN=

# Broker-ACL modunda (pre-signed kullanmıyorsan): node_id identity'den, şifre aşağıdan
# NODE_PASSWORD=
